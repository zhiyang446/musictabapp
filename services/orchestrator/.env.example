# Supabase Configuration
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# JWT Configuration (Optional - for signature verification)
# You can find this in Supabase Dashboard > Settings > API > JWT Settings
SUPABASE_JWT_SECRET=your-jwt-secret-here

# Note: JWT_SECRET is optional. If not provided, the middleware will
# fall back to Supabase API verification which is more reliable but slower.
